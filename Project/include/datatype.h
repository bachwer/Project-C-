#ifndef DATATYPE_H#define DATATYPE_H#include <stdbool.h>struct Date {    int moth, day, yaer;};struct information{    char StudenId[10];    char classroomId[10];    char name[20];//ad    char gender[8];    char email[30];    char phone[20];    char password[20];};struct Classroom {    char classroomId[10];    char teacherId[10];    char classroomName[10];};struct information *student;#endif